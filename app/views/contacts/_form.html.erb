<%= simple_form_for(Contact.new, url: create_contact_path) do |f| %>
  <%= f.error_notification %>
  <%= f.input :username, label: "Nom" %>
  <%= f.input :email %>
  <%= f.input :description, label: "Votre demande" %>
  <%= f.button :submit, "Envoyer", class: "submit-btn" %>
<% end %>